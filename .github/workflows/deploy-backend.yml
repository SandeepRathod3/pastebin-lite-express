name: Deploy Backend (Render)

on:
  workflow_call:
    outputs:
      backend-url:
        description: "Backend URL"
        value: ${{ jobs.deploy.outputs.backend-url }}

jobs:
  deploy:
    runs-on: ubuntu-latest

    outputs:
      backend-url: ${{ steps.save-url.outputs.backend-url }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      # -----------------------------
      # Trigger Render deployment
      # -----------------------------
      - name: Deploy to Render
        run: |
          echo "🚀 Triggering Render deployment..."

          curl -s -X POST \
            -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
            "https://api.render.com/v1/services/${{ secrets.RENDER_SERVICE_ID }}/deploys"

          echo "✅ Deployment triggered"

      # -----------------------------
      # Save backend URL
      # -----------------------------
      - name: Save Backend URL
        id: save-url
        run: |
          echo "backend-url=${{ secrets.RENDER_BACKEND_URL }}" >> $GITHUB_OUTPUT
          echo "BACKEND_URL=${{ secrets.RENDER_BACKEND_URL }}" >> $GITHUB_ENV
